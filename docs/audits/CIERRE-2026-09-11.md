# Cierre final OmniMon — 2026-09-11

Repos:
- App: `/Users/jorge/Documents/Apps/omnimon_apps/macmon` (`CAPDESIS/omnimon`)
- Landing sibling: `/Users/jorge/Documents/Apps/omnimon_apps/macmon/omnimon_landing` (`chochy2001/omnimon_landing`, git aparte, gitignored en macmon)

Un escritor. No Polar. No `--force`. No se borro `target/`. No `cargo test --workspace`.
No se afirma “v6.7.0 en todas las Mac”.

## Git

| Repo | Rama | HEAD | vs origin/main |
| --- | --- | --- | --- |
| macmon | `main` | ver commit de este ledger | ahead (KEEP + cierre) |
| landing | `main` | `ed56bcc225bc91f6a18e18cb72179b86aa66e6dc` | ahead 1 (legales) |

`git fetch --prune --tags` en ambos. Worktrees macmon: solo el cwd.

Landing prune **solo MISSING**:
- Removido: `/private/tmp/fix-omnimon-landing` (gitdir apuntaba a ruta inexistente)
- Conservado (no MISSING): `/Users/jorge/Documents/coord-worktrees/ship-omnimon-landing` detached `ce1a443`

## Ramas

| Rama | SHA | Decision |
| --- | --- | --- |
| `remediacion/omnimon-ci-security` | `cdcb437` = `origin/main` | Ancestro de HEAD. **No merge.** Mismo SHA que `origin/main`; duplicar seria no-op. |
| `origin/fix/ci-coverage-determinism` | `86571c1` | **No merge completo.** El workflow AND `public-only`+`test-light` recrea la cola infinita documentada en `#44`. `h2 0.4.16` ya esta en main. KEEP tomado: tests TUI deterministas (`v4/crates/tui/src/app.rs`). |
| `origin/ci/self-hosted-runners` | `9974906` | **No merge.** Cambia macos/windows de `macos-latest`/`windows-latest` a labels Capdesis privadas. Sin evidencia de que este repo sea aceptado en esos grupos; no es cierre de producto. |

## KEEP (codigo o docs)

| Track | Estado |
| --- | --- |
| C Lua | Hecho en `470f6e4`: `create_plugin_lua()`, stdlib restringida, tests `run_plugin_source_does_not_expose_*`. Docs dejan de llamarlo sandbox. |
| D Landing | Hecho en `ed56bcc`: `/privacy` `/terms` `/es/privacy` `/es/terms`; CookieBanner montado; href reales. |
| B Front | Hecho: empty-state i18n + copy sin “sandbox”. Vitest 703 passed. |
| TUI | Este cierre: `refresh_from_state` + tests inyectados (sin sleep 700ms al watcher). |
| Homebrew | Formula sigue `6.6.6` (ultimo tag publicado). Workspace `6.7.0` no publicado. |

## Validacion (esta sesion)

| Comando | Resultado |
| --- | --- |
| `cd v4 && cargo test -p core -- --test-threads=1` | unit 321 + integration 96, exit 0 |
| `cd v4/apps/desktop && bun run test` | 46 files, **703 passed**, exit 0 |
| `cd v4 && cargo test -p omnimon-desktop --lib run_plugin_source` | 5 passed (stdlib restringida) |
| `cd v4 && cargo test -p omnimon-tui -- --test-threads=1` | 32 passed (incluye `refresh_from_state_*`) |
| `cd omnimon_landing && bun test src/lib` | 19 passed |
| `cd omnimon_landing && bun run build` | 16 pages, incluye las 4 legales |

## Diff vs origin/main (app)

Dos commits de ola + este cierre: Lua/docs/CVE/h2/homebrew/i18n + TUI tests + este ledger.

## Residual

- Tag `v6.7.0` y DMG Homebrew no existen.
- Ramas remotas de CI (`fix/ci-coverage-determinism`, `ci/self-hosted-runners`) no integradas; razones arriba.
- `cargo audit` warnings GTK3/unmaintained (CI no falla en warnings).
- Worktree landing `ship-omnimon-landing` detached en `ce1a443` (pre-legales); no tocado.
- Push ancestro: `git push origin main` en cada repo (sin `--force`).
