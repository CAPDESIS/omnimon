[package]
name = "cli"
version = "4.0.6"
edition = "2021"
description = "macmon CLI application"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
core = { path = "../core" }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
