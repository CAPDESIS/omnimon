<!--
  @deprecated This component is no longer used.
  The sidebar layout was replaced by a single-screen fluid layout.
  SystemDashboard is now rendered inline in App.svelte.
  SmartAlerts is a fixed overlay.
  ProfilePanel is accessible via Settings.
-->
<script lang="ts">
  // Kept for backward compatibility — no-op render
</script>
